class Trip {
  constructor(uuid, tripName, destination, start, end) {
    this.uuid = uuid;
    this.tripName = tripName;
    this.destination = destination;
    this.start = start;
    this.end = end;
  }
}

export default Trip;
